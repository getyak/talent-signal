# V9 independent review brief

Review only artifact `TS-V1-EXPERIENCE-2026-08-25-09`. Verify every inspected
source and screenshot against [`runtime-evidence.json`](runtime-evidence.json)
and [`ui/manifest.json`](ui/manifest.json). Do not inspect or rely on the three
excluded concurrent-change sources. The xcresult summaries and exported PNGs
are the runtime proof for those journeys.

Return the specialist packet required by your skill: reviewer, lens, verdict,
integer score, confidence, findings, strengths, missing evidence, vetoes, and
open questions. Do not average scores and do not convert missing physical,
production, accessibility, or field evidence into a pass.

## Shared questions

1. Does sparse Today establish a clear resting point without hiding owned work?
2. Is Ask genuinely conversation-first while keeping search, Capture, evidence,
   and existing work functional and progressively disclosed?
3. Does the Apple entry/sign-out boundary remain honest about local versus
   production proof?
4. Does any surface rank a person's worth, infer traits, or imply an external
   action occurred?

## Safety re-review

The V8 veto said an old idempotency response could be replayed after a later
review cycle and shown as applied. Verify the V9 request/response contract,
fragment/resource/capture/receipt locks, monotonic database review chain,
current-result-only replay check, iOS prior/result ID validation, protected
recovery, and the real adversarial evaluator. Issue a veto if an R0 operation
can still appear applied after R1→R2, revocation, expiry, or deletion.

## Mobile re-review

The V8 findings asked for exact action arrival and no competing reconciliation
while a request is live. Verify the exact-action screenshot and source path,
the in-flight key guard, restored-pending behavior, canonical timestamp, AX5,
focus/scroll legibility, and no-write boundary. Keep direct-interaction and
physical accessibility evidence separate from screenshot/source inference.

## Recruiter and candidate re-review

Check whether the additional authority mechanics remain quiet by default,
preserve recruiter control and candidate dignity, and avoid adding duplicate
work or misleading success. Call out any operational burden or human-impact
regression introduced by the correction.
