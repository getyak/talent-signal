Synthetic signed-feed rehearsal; ad-hoc app, not notarized.
Initial UI: 0.2.1 (900001), 0.2.2 (900002) available.
Before click HTTP: GET /appcast.xml 200; no archive request.
One native update click, no second confirmation.
Relaunched UI: 0.2.2 (900002), current version is latest.
Installed CFBundleVersion readback: 900002

Actual WorkspaceBrowser/WebKit experiment (synthetic loopback workspace):
The app compiled the unmodified WorkspaceBrowser class and its origin type,
connection resolver, updater/session and isolated bridge from the task sources.
A tiny SwiftUI wrapper hosted that exact class and displayed test diagnostics.
Page-world installDesktopUpdate handler: undefined.
Programmatic page anchor.click(): offer remains available, old build unchanged,
no archive download; synthetic textarea draft remains present.
Reload after display publication: offer and trusted click listener restored.
One real account-footer click: UI changes to preparing/update-in-progress,
no second application confirmation.
Relaunched WebKit UI: 0.2.2 (900002), current version is latest; footer update control absent.
Installed bundle readback after footer click: 900002

Tampered archive experiment:
Restored the task-owned synthetic installed bundle to 900001, preserving the
rehearsal trust key; changed one byte of the served 900002 ZIP without resigning.
One footer update click was rejected; UI reported recoverable update failure.
Installed bundle still read 900001. The retry link performed only another check,
returned to an available offer with a new token, and did not restart or install.
No production app, account, data or trust settings were modified.
