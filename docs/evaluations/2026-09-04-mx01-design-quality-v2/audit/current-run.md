# Current-run audit

Checked on September 4, 2026 against the frozen candidate in
[`freeze-candidate.json`](../freeze-candidate.json).

## Direct evidence

- `current-run/today.png` shows the lead card keeping the primary CTA visible in the first viewport while preserving person, dependency, source, and effect boundary.
- `current-run/fact.png`, `approval.png`, `unknown.png`, and `verified.png` show one continuous source → fact → effect → outcome loop instead of disconnected status pages.
- `current-run/ambiguous.png` keeps identity conflict explicit, disables the historical attachment path, and preserves a no-create boundary.
- `current-run/no-action.png` and `insufficient-ax5-dark.png` keep truthful no-action and insufficient-evidence states without manufacturing urgency, including AX5 dark reduced-motion coverage.
- `current-run/console-2026-09-04.json` records a fresh zero-warning, zero-error console check from a new browser tab.

## Current-run conclusions

1. The selected source visual's strongest qualities are now preserved in the build: quiet editorial type, restrained vermilion only at consequence, one evolving object, and adjacent provenance.
2. The most important behavioral regression from the earlier code review is closed: a non-lead continuation replay now opens Maya Patel / VP Engineering · Atlas rather than falling back to Alex / Aurora.
3. No active wrong-identity, unsupported-fact, unauthorized-write, duplicate-retry, inaccessible-consequential-path, or candidate-ranking issue was observed in the frozen screenshots or browser replay.

## Remaining boundary

The bundle is strong enough for a DQI pass, but it still does not replace the
separate real-human MX-01 comprehension gate.
