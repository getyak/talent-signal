# GET-9 Memory-to-original-image follow-up

Status: this slice passed independent code review and its synthetic product
scenario. Installed-client acceptance and general file/code/browser capability
delivery remain open. It is not overall GET-9 completion.

## Product evidence

| Trial | Outcome | Independent assessment |
| --- | --- | --- |
| [First](completeness-memory-image-first.json) | Model interpreted the fixture's verification-code wording as a login credential and declined image inspection; unreviewed/unattributed setup also failed the existing completion-source fence | Failed; no image-tool proof or accepted answer |
| [Second](completeness-memory-image-second.json) | Reviewed synthetic document-reference source; real fresh-session HTTP chat reads Memory, original overview, tile 6, selects the fragment citation, and returns the exact identifier | Task 4, grounding 4, naturalness 4, recovery not exercised |

The second fixture uses an ordinary document reference, not an authentication
code. The random identifier is deliberately absent from the stored excerpt and
chat objective. Setup files one synthetic screenshot through the product tools
using a scripted source-ingestion adapter, then confirms its exact excerpt and
speaker through the existing evidence-review API. Chat uses the actual SDK and
production adapter, HTTP, PostgreSQL and source storage. This distinction is
explicit in the evaluator and is not claimed as an autonomous image-ingestion
or installed-client test.

All 13 execution checks pass in 17.263 seconds. The answer reports
`MEM-59e2c268`, which matches the native original, and cites its original fragment.
There is no SDK mirror for the fresh product Session. Revocation returns HTTP
409 for the image and 410 for the saved answer readback. Source pixels are not
duplicated into this receipt; image dimensions, hashes and tool provenance are
retained. The owned temporary image directory was removed; synthetic database
evidence remains.

## Reviewed boundaries

- Fragment IDs come from the current governed Memory manifest. Reads resolve
  fragment → resource → capture → owned screenshot task → exact image index.
  Account, relationship, uploader, source status and original hash remain bound.
- A Run with image capabilities is ephemeral. Both adapter and shared Harness
  prevent durable continuation; the MCP output boundary also denies unexpected
  images in an otherwise persistent Run.
- Tool-discovered image guards join adapter and backend Run authority checks.
  The earliest admitted task/image expiry bounds observations and product runs;
  host guards retain only IDs/hash, not an extra pixel cache.
- The Harness returns pixels to the SDK but projects image observations to
  metadata. Product diagnostics also omit MCP image data. Final source failure
  retains actual token usage while completing continuation/observation as failed.
- All ordinary SDK chat completions recheck authority after the existing source
  lock and before idempotent completion, including requests without a previous
  answer. No source review, permission, schema or quality gate was relaxed.

Independent review found and closed three P1 lifecycle/retention gaps plus a
later P1 finalization gap and a P2 closure-retention issue. Independent focused
checks pass 36/36. Parent checks: Agent 177 pass / one explicit skip; PostgreSQL
21/21; Agent typecheck and backend build pass. The PostgreSQL regression lets
an image naturally expire after the provider returns, then verifies final
rejection and rollback of the answer manifest.

Earlier test failures were fixture problems and are retained in local logs:
malformed synthetic MCP base64; a missing Wiki compilation; reused constant image
bytes treated as a duplicate source; and a missing explicit evidence/speaker
review. The final fixture has unique source identity and uses the real review
operation. These corrections did not change production acceptance requirements.
