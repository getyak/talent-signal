# GET-40 phase 1 · three-scope Memory backend, contracts, Agent integration

## Outcome

Deliver the real backend half of GET-40: one bounded Memory review domain in the
existing PostgreSQL/Fastify backend with three logical scopes (`self`, `person`,
`relationship`), additive contracts, typed Agent recall/stage integration in
both the ordinary conversation and the scoped relationship Ask, and
deterministic unit + PostgreSQL integration tests. A human commit is the only
way accepted memory or a contact is created; models can only recall and stage.

This phase does **not** deliver the shared Web UI, native iOS UI, production
runtime acceptance, real-model evaluation, or the visual three-state component.
Those remain for the parent/second phase and are listed under "Remaining".

## Source of truth

- Requested behavior: Notion v0.2 "三域 Memory · 截图到增量确认 v0.2"
  (`/Users/cubxxw/.local/state/talent-signal-get40/notion-v02-source.md`).
- Parent implementation decisions:
  `/Users/cubxxw/.local/state/talent-signal-get40/backend-design.md`.
- Frozen extra cases for the parent's later real-model phase:
  `/Users/cubxxw/.local/state/talent-signal-get40/frozen-cases.json`
  (not run here; no model credentials).

## In scope (implemented)

- `apps/backend/src/database/076_memory_review.sql`,
  `077_memory_review_corrections.sql`, manifest, system-health, and app-test
  wiring.
- `packages/contracts/src/memorySchemas.ts`; additive `memory_proposal` event
  and `ChatTaskResponse.memory_proposal`.
- `apps/backend/src/modules/memoryReviewPolicy.ts` (pure rules).
- `memoryReviewStore.ts` (rows, provenance, contact-status, host source
  authority, lineage, invalidation helpers).
- `memoryReviewStage.ts` (stage + guarded rebase).
- `memoryReviewRead.ts` (purpose/credential-bound review, draft merge).
- `memoryReviewCommit.ts` (commit, receipt, readback, undo, correction).
- `memoryReviewRecall.ts` (recall + explicit-deletion propagation).
- `memoryReviewRoutes.ts` + `memoryReview.ts` barrel + `app.ts` registration.
- `apps/agent/src/memorySchemas.ts`, `toolCatalog.ts`, `types.ts`,
  `claudeChatProvider.ts`.
- `workspaceConversationAgent.ts` (ordinary conversation typed recall/stage),
  `chat.ts` (scoped relationship Ask typed recall/stage via the same domain).
- Source-lifecycle hooks in `captures.ts`, `identityCorrections.ts`,
  `agentSessions.ts`, `chatMedia.ts`.
- Tests `memoryReview.test.ts`, `memoryReview.integration.test.ts`,
  `apps/agent/src/memoryReviewTool.test.ts`.
- ADR `docs/decisions/0017-three-scope-memory-review.md`, narrow canonical-doc
  wording, and `docs/evaluations/get40/phase-1-backend-report.md`.

## Out of scope

- Web People/relationship/Pursuit review surfaces, Chat review card, mobile
  sheet/anchor panel, 390px/1280px screenshots.
- iOS/native client changes.
- Production deployment, migrations on real data, model credentials.
- Vector memory, a second database, or a general prompt-memory store.

## Model

- `memory_items` — accepted memory: one independently understandable statement,
  scope, explicit subject/relationship dependence, statement kind (`fact`,
  `source_statement`, `user_opinion`), speaker/reporter, valid/observed time,
  version, supersede/conflict links, sensitivity, status.
- `memory_item_evidence` — independently retained minimal excerpt + locator +
  original lineage (capture/resource/fragment or Session/message/artifact).
  Natural Session/image TTL does not revoke it; explicit Session, capture, or
  media deletion, authorization revocation, identity rebind, or item deletion
  does.
- `memory_proposals` / `memory_proposal_items` — frozen staged candidates with
  `subject_kind` (`owner_self`/`resolved_subject`/`proposal_target`),
  `relationship_kind`, per-item admission + `judgment_kind`, default selection,
  previous target/version, provenance and exact source excerpt.
  `added_revision` freezes late additions out of an open review.
- `memory_review_scopes` — server-issued binding of reader, purpose, proposal
  revision, allowed item set, source Session/message, and credential hash.
- `memory_review_drafts` — resumable, non-canonical selection/edits/decisions
  keyed by proposal, merged per surface so a restricted save never drops
  private Chat choices.
- `memory_commits` / `memory_commit_items` / `memory_receipts` — idempotent
  receipt, decision ledger, undo token and limits.
- `memory_proposal_rebases`, `memory_projection_jobs` — guarded identity rebase
  log and Wiki/index rebuild outbox. Accepted memory immediately supersedes
  affected knowledge snapshots and bumps the harness source generation.

## Decisions

1. Recall is scope-first and authentication-first: `self` is private to the
   acting user; `person`/`relationship` need an active subject/context and are
   never serialized on a restricted surface.
2. `contact_status` is derived from the account directory. A new-contact name is
   a source-grounded draft (`proposal_target`), not an asserted identity; a
   same-name directory conflict is `ambiguous`. A stable identity clue derives
   a deterministic `subjects.external_ref`, so a concurrent duplicate create
   fails closed.
3. Commit is all-or-none per review scope. A skipped contact saves only `self`
   items and leaves person/relationship items pending so restoring the same
   identity recovers the prior selection. A partial commit bumps the proposal
   revision and rebases the remainder.
4. Conflicting/sensitive/ambiguously-attributed items are not default-selected
   and need an explicit typed decision. Any edited display text is conservatively
   user-edited with the original quote retained.
5. Undo is version-bound compensation; it refuses to overwrite a later version
   and never revives a version whose source is no longer authorized. Contact
   reclaim checks captures, memory, assignments, handles, profiles, manifests.
6. No regex-only business extraction. Candidates are staged by the bounded model
   through the typed Agent tool; the server validates grounding, dependence,
   admission, duplicates, and target revisions.

## API handoff (parent builds UI/runtime acceptance on these)

All authenticated `bearerSession`, account-scoped, additive to existing
contracts. Exact routes live in
`apps/backend/src/modules/memoryReviewRoutes.ts`.

- `GET /v1/memory/proposals?purpose=&person_id=&relationship_context_id=` —
  purpose-scoped list; people/relationship require the exact contact and never
  return private self counts or ids.
- `POST /v1/memory/proposals` — untrusted staging request. The server loads the
  authenticated Session message/ordered images itself; the body cannot carry
  source text or artifact authority.
- `POST /v1/memory/proposals/:proposalId/reviews` — open/reuse a purpose-bound
  review scope and return a one-time `review_credential`.
- `POST /v1/memory/proposals/:proposalId/rebases` — guarded identity rebase.
- `GET /v1/memory/reviews/:reviewScopeId?credential=` — read the frozen view.
- `PUT /v1/memory/reviews/:reviewScopeId/draft?credential=` — merge local draft.
- `POST /v1/memory/reviews/:reviewScopeId/commits?credential=` — atomic commit.
- `GET /v1/memory/operations/:operationKey` — receipt readback; a revoked source
  returns `source_revoked` instead of the old payload.
- `POST /v1/memory/commits/:commitId/undo`
- `GET /v1/memory/items?surface=&person_id=&relationship_context_id=&limit=`
- `POST /v1/memory/items/:itemId/mutations` — versioned correct/delete.

Response types are the additive `packages/contracts/src/memorySchemas.ts`
exports. Unscoped Chat agent events add `memory_proposal`; scoped relationship
Ask adds `ChatTaskResponse.memory_proposal`.

### Synthetic fixture

Stage a real proposal and read it back through the same domain, e.g. with the
synthetic DB and a logged-in test account:

- create account/user, then `POST /v1/memory/proposals` with `surface:"chat"`,
  `session_id`/`source_message_id` of a real Session turn, `contact_decision`
  (`existing`/`new`/`none`), and candidate items whose `source_excerpt` is an
  exact substring of the admitted message (image items cite an admitted
  `image_region.artifact_id`).
- `POST /v1/memory/proposals/:id/reviews` with the surface purpose; use the
  returned credential for draft/commit/read.
- Commit with `contact_decision:"new"` and `new_contact` to create one Person
  and 17 items in one batch.

## Migration inventory record

The reviewed migrations number four (`076_memory_review`,
`077_memory_review_corrections`, `078_memory_review_hardening`,
`079_memory_source_authority`). The migration-inventory record in
`scripts/check-architecture-boundaries.mjs` was advanced exactly as that
checker instructs:

- `FROZEN_MIGRATION_COUNT = 84`
- `FROZEN_MIGRATION_DIGEST = "15a86e33c72a2d1fed2dd49e963a069856a04044ed8ab48958a53707ae29a1a3"`

This only records the appended migrations; it does not relax the hotspot,
boundary, or migration-history checks. The migrations are manifest-registered
and applied only to the synthetic database.

## Source-authority correction (round 2)

Pending review authority is now separate from accepted retained evidence:

- Pending reads, drafts, commits, and reproposals reconstruct the original
  admitted source and reject when the exact Session/message is deleted,
  expired, purged, or has changed message text or ordered image
  attachment/digest. `visibleReviewItems` drops items whose source is no longer
  available instead of returning expired pending text.
- Commit revalidates the proposal source snapshot and every selected item under
  `FOR SHARE` row locks on the original Session, conversation images, captures,
  resources, retention receipts, and fragments. This runs even for a
  contact-only commit (`selected.length === 0`).
- Accepted minimum evidence keeps independent retention: a commit before
  natural TTL survives recall; an explicit Session/capture/artifact deletion or
  authorization revocation immediately removes dependent memory.
- `memory_source_revocations` is epoch-aware via `captures.version`: an
  explicit delete writes an all-epoch tombstone, while an explicit revoke or
  identity rebind tombstones only the revoked capture version so a later
  restore/rebind can stage fresh evidence while the old epoch stays gone. The
  `correctCaptureIdentity` hook reads and locks the old capture version before
  recording the old-epoch tombstone. Natural source-authorization expiry does
  not invalidate accepted Memory.
- `rebaseMemoryProposal` preserves the draft and reactivates items for a
  restored target; `regenerateMemoryProposal` runs the bounded proposer outside
  the SQL transaction and version-guards the final write, keeping the
  relationship label in `relationship_display_label` rather than a UUID column.
- Review scopes now hold multiple independent credentials
  (`memory_review_credentials`), so a second same-revision tab no longer
  invalidates the first; a stale commit still reconciles on revision.
- A source/revision-scoped dismiss action marks reviewed items declined without
  creating memory, a contact, or a commit receipt.

## Semantic correction (round 3)

- Temporal non-destructiveness: `classifyTemporalRelation` returns `separate`
  when a new future plan meets a non-future fact; stage derives effective
  `operation = add` and clears the previous target so the plan never supersedes
  the fact. A future plan replaced by new evidence of completion still updates,
  and same-plan reschedules still update. Commit persists the effective
  operation, so stage/review/commit agree.
- Pending-contact self escape: candidates declare `dependence_kind`
  (`independent_self` / `contact` / `relationship`), so a contact-dependent
  sentence is bound to the proposal target even before a UUID exists. A host
  consistency check flags an undeclared self sentence that names the current
  contact/identity clue, and commit applies the same invariant to edited text.
- Explicit revoke/restore: `decideCaptureSourceAuthorization` now calls
  `invalidateMemoriesForCaptureIds(..., 'source_revoked', oldVersions)` in the
  same transaction, permanently revoking accepted Memory evidence and
  tombstoning the revoked capture version; restore leaves the old epoch gone,
  while a new epoch can stage and accept fresh evidence.
- Pending capture deadline: the pending verifier now requires the current
  source-access authority (`authorization_state = authorized`,
  `source_access_state = available`, future `authorization_expires_at` and
  `retention_until`), so a passed deadline fails review closed before the sweep;
  accepted minimum evidence still survives natural TTL.
- Pure-image host: the raw user source text is separated from the provider
  instruction, so an images-only message persists an empty user objective, the
  proposal binds the empty-message snapshot, and the real host stages and
  commits 17 items.
- Relationship host: the Chat relationship text artifact is bound to the
  Session message id (not a fabricated media-looking string), and the verifier
  decides media vs session authority by real `chat_media_assets` rows rather
  than string guesses.
- Regeneration: the regenerator receives the ordered admitted images, runs the
  provider outside SQL, regenerates for both existing and new identity changes,
  preserves choices on same-identity restore, and records a real failure without
  writing when the model is unavailable.
- Idempotent scope binding: the commit idempotency request includes the review
  scope id and replay verifies the committing scope, so an operation key cannot
  be replayed through another legitimate scope to return private ids.
- Helpful answer: an authorized search/read/recall/stage may now end in a
  helpful answer (with the resolved-contact provenance event and an optional
  memory reference) instead of forcing `use_contact`.
- Conflict undo: undoing a conflict batch no longer clears the conflict group
  when a later active conflict still shares it.

## Verification

- `pnpm --filter @talent-signal/backend typecheck`
- `pnpm --filter @talent-signal/backend exec vitest run src/modules/memoryReview.test.ts`
- `DATABASE_URL=postgres://get40_test@127.0.0.1:55440/get40_test pnpm --filter @talent-signal/backend migrate`
- `CONTACT_AGENT_TEST_DATABASE_URL=postgres://get40_test@127.0.0.1:55440/get40_test pnpm --filter @talent-signal/backend exec vitest run src/modules/memoryReview.integration.test.ts`
- `pnpm --filter @talent-signal/agent test`
- `node scripts/check-docs.mjs`, `git diff --check`

The integration suite now has 46 real PostgreSQL tests, including the round-3
additions: future-plan non-destructiveness, plan-to-completion update,
contact-dependent self escape (declared and undeclared) plus edited-text
refusal, explicit revoke/restore with new-epoch acceptance, pending capture
deadline, the real empty-objective image-only host path, the session-bound
relationship host artifact, existing-contact regeneration with failure
preservation, idempotency scope binding, conflict undo preservation, the
authenticated POST rebase route, and a provider-wait race where a source delete
or image removal during inference rejects with no write.

## Remaining after this phase (not claimed as done)

- Shared Web review surface and three visual states (fold/inline/loose note),
  plus Chat/People/Pursuit entry points and 390px/1280px evidence.
- No native UI or runtime change was requested; the response contract stays
  additive and the memory reference is not an `agent_event` kind.
- Real-model A/B/C value evaluation on the frozen cases, runtime screenshots,
  independent implementation review, production deployment.
- Phase 2 (parent-scheduled): conversation-queue completion and Session
  restoration lifecycle acceptance, an authenticated HTTP `inject` round trip
  for the POST rebase regeneration route, and scope-bound operation
  recovery/undo.
- Parent advance of the architecture migration-inventory count/digest to four
  additional entries.
## Phase 2 · shared Web review surface, scoped recovery, fixtures (checkpoint)

Status: implemented and locally verified by the worker. This is a checkpoint
for the parent's browser inspection, not final product acceptance. No real
browser, real-model, or human-value verification is claimed here.

### What is now implemented

- Shared production review card (`apps/web/components/memory-review/`) rendered
  under the completed Chat turn and on the person-only route. It uses Radix
  Dialog, existing workspace tokens, a pure draft reducer, and a server-issued
  purpose-bound review credential.
- Same-origin BFF `apps/web/app/api/memory/[[...path]]/route.ts` with the
  current rendered login binding, strict route allowlist, `no-store` reads, and
  no browser-supplied account authority. The owner-only raw
  `/v1/memory/operations/:key` and `/v1/memory/commits/:id/undo` paths are not
  exposed through the BFF; only the scope-bound operation views are.
- Session restoration: `conversationQueueCompletion.ts` and
  `workspaceChat.ts` persist the additively typed `memoryProposal` reference on
  the completed turn, so a closed/restored Session re-opens the same review.
- Relationship Ask admission: `localBackend.askRelationshipChat` now creates an
  owned relationship Session, passes stable session/message IDs to the host,
  and persists the real completed turn only after source persistence; a
  persistence failure returns a truthful retryable error.
- Person-only route `/workspace/people/[id]` and directory links retain the
  person id; existing relationship contexts link explicitly from that page.
- Scope-bound operation recovery and undo:
  `GET /v1/memory/operation-views/:operationKey` and
  `POST /v1/memory/operation-views/:operationKey/undo`. Whole-batch undo is
  allowed only when every receipt decision is visible in the current authorized
  entry scope; a mixed Chat batch is projected without private self ids and
  cannot be undone from a relationship surface.
- Pursuit scope resolver `GET /v1/memory/pursuits/:pursuitId/scopes` reuses the
  existing reviewed-evidence, bound-identity, and current source-authorization
  predicates; no evidence context degrades to person-only navigation.
- Source availability metadata on the review view (`source_status`,
  `source_unavailable_visible_item_count`) so the UI can distinguish a revoked
  source from zero selected or contact-only.

### Local start commands (parent browser checkpoint)

Synthetic PostgreSQL is owned at
`postgres://get40_test@127.0.0.1:55440/get40_test`. Do not touch the resident
4317/4318 services.

```bash
# 1. Apply migrations to the synthetic database.
DATABASE_URL=postgres://get40_test@127.0.0.1:55440/get40_test \
  pnpm --filter @talent-signal/backend migrate

# 2. Seed the synthetic Session + real staged proposal (mode-0600 receipt).
node scripts/evals/get40/seed-fixtures.mjs \
  --database postgres://get40_test@127.0.0.1:55440/get40_test \
  --backend-base-url http://127.0.0.1:55442 \
  --web-origin http://127.0.0.1:55443 \
  --output /private/tmp/ai-test-get-40.G6UF0E/get40/fixture-receipt.json

# 3. Start the isolated fixture backend on 127.0.0.1:55442.
DATABASE_URL=postgres://get40_test@127.0.0.1:55440/get40_test \
  node scripts/evals/get40/fixture-server.mjs

# 4. Start the Web host on 127.0.0.1:55443 against that backend.
TALENT_SIGNAL_BACKEND_URL=http://127.0.0.1:55442 \
  pnpm --filter @talent-signal/web dev --port 55443
```

Then sign in through the production `/login` with the synthetic account from
the receipt file (`username`/`email`/`password`; the file is mode 0600 and must
never be committed or copied into documentation). Open the receipt's
`webSessionUrl` to inspect the folded card, inline rows, and loose-note sheet,
then save/undo through the production BFF.

The current meaningful fixtures (regenerate with the seeder command above;
each scenario uses its own synthetic account so accepting one cannot mutate
another):

- `main17` — production queue admission + deterministic fixture provider invoking the real `memory_review` tool, two original JPEG screenshots, a neutral organizing relationship label, a useful nonempty answer, real completed Session + Memory reference:
  `http://127.0.0.1:55443/workspace/sessions/7013064d-1e8c-4d52-80ac-2b006f9cbda2` (proposal `66c19c7f-512d-4109-9082-1cc8817665a5`, rev 1, 17 items).
- `text17` — text-only direct-domain source for independent text cases:
  `http://127.0.0.1:55443/workspace/sessions/1642abd7-be39-4dcd-959a-9076f539c9b9` (proposal `de47558a-bcfa-4bea-a1f4-565cd9365e50`, rev 1, 17 items).
- `contactonly` — independent account for the parent's contact-only action:
  `http://127.0.0.1:55443/workspace/sessions/1626132e-1bb3-4f4b-9863-2dd6b8b549b4` (proposal `e297dca3-b956-40c7-8095-2a1420074010`, rev 1, 17 items).
- `skiprestore` — independent account for skip + restore of the same identity:
  `http://127.0.0.1:55443/workspace/sessions/199054fd-a16d-460e-9dc1-ae94bc38dd21` (proposal `f85be238-209f-4329-93e7-b3e44ff83a1a`, rev 1, 17 items).
- `dismiss` — independent account for whole-card and scoped dismiss:
  `http://127.0.0.1:55443/workspace/sessions/017f56e0-af27-4066-b2ff-01aaa531acfb` (proposal `c840138f-0d44-47bc-a4a2-f5abdf2e25ec`, rev 1, 17 items).
- `undo` — independent account for save then undo:
  `http://127.0.0.1:55443/workspace/sessions/59aaa213-ed9e-490a-975e-951e93be8cc0` (proposal `c10f3b52-8397-4a1d-bf67-d317b7499864`, rev 1, 17 items).
- `sourceexpired` — source replaced after staging, so the card must truthfully disable the affected save:
  `http://127.0.0.1:55443/workspace/sessions/42aef56a-b915-4558-a308-2f392585440f` (proposal `e985782f-639b-4bdf-8d9d-414d8c78eca9`, rev 1, 17 items).

Credentials are only in `/private/tmp/ai-test-get-40.G6UF0E/get40/fixture-receipt.json` (mode 0600).

Memory review requests now require the server-signed entry capability minted by the
actual Web entry (Chat Session, People person, Pursuit role/evidence); the browser
cannot widen the signed purpose or target.

The `main17` provider is scripted fixture evidence only, never actual model
quality; it stages the exact `seventeen-source.json` items (future plans marked
`time_status: future`), supplies a neutral organizing relationship label, and
returns a meaningful answer without fabricating a completed response.

Re-running the seeder creates fresh independent accounts and sessions so a
previous accepted/discarded state cannot contaminate a later review. The seeder
retries a transient first-entry worker race in a fresh Session.

### Actual model evaluation (parent-run, bounded)

```bash
pnpm --filter @talent-signal/backend build
HAO_ANTHROPIC_API_KEY="$(existing dev:/shared injection)" \
  node scripts/evals/get40/evaluate-model.mjs \
    --endpoint https://api.hao.ai/anthropic \
    --model anthropic/claude-sonnet-5 \
    --database postgres://get40_test@127.0.0.1:55440/get40_test \
    --cases /Users/cubxxw/.local/state/talent-signal-get40/frozen-cases.json \
    --output /private/tmp/ai-test-get-40.G6UF0E/get40/model-evaluation.json \
    --max-estimated-usd 1.00 --max-cases 20
```

The evaluator refuses non-loopback databases and non-admitted endpoints, keeps
frozen inputs unchanged, reports actual tokens/cost or unknown, and stops at
the budget guard or on unavailable usage. The worker did not run paid cases.

### Verified by the worker

- `pnpm --filter @talent-signal/web typecheck`, `lint`, `test` (1163 passing).
- `pnpm --filter @talent-signal/backend typecheck`.
- `CONTACT_AGENT_TEST_DATABASE_URL=postgres://get40_test@127.0.0.1:55440/get40_test pnpm --filter @talent-signal/backend exec vitest run src/modules/memoryReview.integration.test.ts` (49 passing) and `memoryReview.test.ts` (7 passing).
- Fixture seeder and fixture server run against the synthetic database; the
  evaluator smoke run with `--max-cases 0` writes a mode-0600 report.
- `pnpm docs:check`, `git diff --check`.

### Not done / caveats

- No real browser screenshots, recordings, paid model run, or human value
  verification; the parent owns those.
- Backend hard gates still open: bounded image-evidence identity clue and
  same-name recall authority; persisted Pursuit/role/evidence association
  revalidated inside the commit/undo transaction.
- The relationship-desk surface still needs the same scoped card entry that
  People detail and Pursuit now use.
- Fixture scenarios beyond `main17`/`text17`/`contactonly`/`skiprestore`/
  `dismiss`/`undo`/`sourceexpired`: a `>20` group search scenario and an
  identity-change success/failure scenario.
- The evaluator seeds accepted state through the production domain and stops
  truthfully on timeout or unknown usage, but the worker ran no paid case.

### UI completion round (2026-09-22, after parent browser review)

- Visible pending new-contact header with "本次截图 · 待添加", "换个人" and
  "暂不添加联系人"; self-only drafts omit the header.
- Folded overview is one group checkbox + title/count/caret + one full-sentence
  preview disclosure; expanded shows up to four choices then a remaining link.
- Server draft persistence with `expected_review_revision`, debounced and
  sequence-guarded, with 409 re-read/rebases and local-edit preservation.
- Change-person flow uses the authenticated People search and the real rebase
  route with `human_selection`; failed rebases keep the original target.
- Portal carries the workspace token scope and sits above the mobile nav.
- Web suite 1170 passed / 1 skipped; backend memory integration 49 passed.

### Parent takeover — 2026-09-22

The user explicitly authorized Codex to implement the remainder after Pi stopped at turn826 with provider HTTP402. The parent is now the only writer in this existing worktree. Independent sub-agent review remains required. Partial checks (55 PostgreSQL Memory,36 Web Memory,39 Agent tests and Web typecheck) passed, but entry authority, current identity evidence, atomic Pursuit replay/undo, final UI lifecycle, evaluator and runtime acceptance remain unfinished. No production/native release or issue closure is authorized before the stated acceptance gates.

## Parent runtime acceptance checkpoint — 2026-09-22 08:10 Asia/Shanghai

The parent personally implemented the remaining changes and review fixes after
Pi's provider balance failure. The current authoritative acceptance report is
[phase-2-web-report](../docs/evaluations/get40/phase-2-web-report.md); it supersedes
pending lists in the earlier checkpoints above. Synthetic actual browser images,
four recordings, model failure reports and database readback are versioned under
`docs/evaluations/get40/runtime/`.

Functional evidence now includes real 17→16 save/reload, image-only mobile review,
response-loss reconciliation with one commit, guarded undo, 6-item Pursuit partial
save leaving 11 in Chat with the prior unchecked item preserved, 21-item search
and focus restoration, zero/contact-only/dismissal, invalidated source, identity
failure and regeneration, and consecutive fresh New Chat bootstrap navigation.
Latest local suites: Agent 290 pass/1 skip; backend 111 pass; Web1195 pass/1 skip;
Web lint/build pass. Remaining minor copy layout change is under final rebuild.
Independent reviewers found no unresolved P0/P1 in the reviewed security slice;
final re-review of navigation/re-open feedback corrections is pending.

Actual model evidence is incomplete: initial20 lost all tools due pinned SDK
Zod conversion, now fixed by an equivalent open-object schema plus actual wire
regression. Corrected run reached5 then stopped after token ceiling with unknown
usage. Three early quality failures, a bound-turn fixture persistence/read-scope
error and final prompt/evaluator corrections are recorded, not counted as passes.
No further paid model calls after the unknown-usage stop. Do not close the issue,
merge for release, or deploy. Prepare a reviewable draft PR and retain exact gaps.

Remaining delivery actions: final copy/build verification, reviewer closure,
latest-main integration, docs/architecture checks, commit/draftPR/currentCI,
Notion implementation evidence backlink, issue In Progress readback, owned test
service cleanup. A completed budgeted real-model evaluation remains a separate
necessary acceptance step; A/B/C and human value are explicitly unmeasured.

## Final local review — 2026-09-22 08:24 Asia/Shanghai

Merged origin/main b5f85a5e without conflicts. Parent fixed two additional review
findings: unsaved self intent across identity regeneration/re-open failure, and
persisted contact decisions across real target replacement. The rebase transaction
now keeps only stable self draft intent, updates its decision, and rejects stale
scope draft writes. Independent read-only review found no remaining P0/P1.
PostgreSQL + host tests: 114 passed (72 + 42). Backend build passed. Web: 1196
passed/1 skipped; Agent: 290 passed/1 skipped. Final mobile visual inspection
led to a CSS-only header adjustment so actions no longer truncate contact context.
Real-model acceptance and human value remain incomplete; no paid restart.
