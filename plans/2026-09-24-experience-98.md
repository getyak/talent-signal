# Experience quality improvement loop

## Outcome
Audit and improve Talent Signal end to end through Pi implementation and independent Codex review. Target at least 98/100 in most core journey scorecards, with explicit evidence and unresolved deductions. This target is not a claim of measured human satisfaction.

## Boundary and baseline
- Frozen source baseline: `159c640286c14fa3c1d0244db3f318bf9b30422c` (fetched origin/main).
- Parent worktree: `/Users/cubxxw/.codex/worktrees/talent-experience-98/talent-signal`.
- Preserve dirty original checkout and all resident services. Use synthetic isolated test data for writes. No messages or consequential external effects.
- Begin with Web/responsive, frontend/backend correctness, and desktop shell. Native iOS/macOS remain explicitly unverified until their relevant runtime evidence is collected.

## Milestones
1. Baseline frozen; eight Web page families captured in both themes at desktop/narrow widths. First backend Pi packet independently reviewed; 45 focused tests executed against disposable PostgreSQL.
2. **Active:** Pi task `20260924-154828-f43aa4f5` is revising its first frontend packet after parent review found partial-success identity duplication and mixed-query navigation gaps. First packet: 1,229 tests passed, one skipped; it is not accepted yet. Parent's backend failure-injection retest passed 47 checks, with backend and Agent host typechecks passing.
3. Independent specialist/code review; repair and retest affected paths.
4. Record per-journey evidence, deductions, deep optimization directions and remaining gates; repeat on remaining high-impact gaps.

## Current gates

- EXP-09: whole-API stalls during source admission. The pinned SDK's synchronous
  diagnostic report performs socket-dependent reverse DNS. Two isolated Linux
  report pairs measured about 3.1 seconds before versus 2.4–4.6ms after disabling
  network collection. Runtime entry points are patched; real deployment and
  concurrent source/health readback remain required.
- EXP-10: source actions submit a stale terminal revision. Direct browser
  evidence shows revision 13 submitted while current revision is 14. Reconcile
  state and require a fresh human decision; no automatic identity/deletion write.
- Storage guard last read 73 GiB free, below its 80 GiB heavy-build minimum.
  Continue lightweight checks; preserve other tasks and their artifacts.
- Resident services remain on the observed original image; no deployment, push,
  PR or merge is claimed. Native acceptance and final journey scorecards remain
  open. No overall 98/100 claim is supported.

## Evidence and scoring
Evaluate sign-in/settings, conversations, People/Memory, Today/time, captures/source review, and recovery. For each: task completion 25, state/recovery 25, clarity 15, visual craft 15, accessibility 15, responsiveness 5. A point is earned only from an enumerated check; unknowns cannot pass. Privacy, unauthorized writes, wrong identity, loss, duplicate effects, false success or inaccessible consequential paths veto the journey regardless of points. Specialist 0-4 rubric scores remain separate and are never averaged into this scorecard. Scores are internal rubric results, not field-user certification.

## Completion evidence
Before/after screenshots at desktop and narrow viewports, concrete interactions, reload/readback, theme/keyboard/long-content checks, bounded failure tests, appropriate frontend/backend checks, independent review, and a list of unverified requested surfaces. Store durable evidence in `docs/evaluations/experience-98/`; transient test output uses the storage guard. Do not mark the Goal complete while material requested work remains.
