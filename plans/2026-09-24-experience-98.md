# Experience quality improvement loop

## Outcome

Audit and improve Talent Signal end to end through Pi implementation and
independent Codex review. Target at least 98/100 in most core journeys, using
real interaction and reproducible evidence. This is not a claim of measured
human satisfaction.

## Current status

Goal status was read back as **blocked**. This is a local checkpoint, not
completion. Implementation and independent review finished at source head
`49b16a8c6ee1ee89c73e99dca621e6de143edf25`; no Pi worker remains active.
No push, PR, merge or resident deployment was performed by this task.

- Parent worktree: `/Users/cubxxw/.codex/worktrees/talent-experience-98/talent-signal`.
- Branch: `codex/experience-98`.
- Frozen baseline: `159c640286c14fa3c1d0244db3f318bf9b30422c`; integrated
  `origin/main` at `88eb33b3` (GET-49 / PR #242).
- Preserve the dirty original checkout, other tasks, resident services and the
  user browser group. Only synthetic isolated fixtures were used for writes.
- Until the outstanding boundary questions are answered, do not start new
  account sessions, credential operations or resident deployments.

## Completed local evidence

1. Eight Web page families were captured at desktop/narrow widths in both
   themes. Interaction checks covered creation, lost receipts, exact replay,
   source conflict and withdrawal, Time editing/deletion, search and keyboard
   navigation. Native Chrome 200% zoom was checked on selected populated routes;
   it does not establish full accessibility coverage.
2. Pi implementation plus parent review repaired admitted-message retention,
   stop/restart races, source admission validity, uncertain-result recovery,
   navigation continuation, canonical Person search and shared-clock hydration.
   Typography, narrow layout, touch targets and error states received scoped
   repairs. [Audit index](../docs/evaluations/experience-98/README.md).
3. The independent reviewer found two P1 and three P2, then verified closure of
   the confirmed code defects. Its follow-up found a 21–40 candidate edge;
   failing-before/passing-after 20/21 controls and a final static review closed
   that edge at `49b16a8c` without silently dropping candidates.
   [Resolution ledger](../docs/evaluations/experience-98/review-resolution.md).
4. At `7a99f873`, complete Web tests passed 1,276 / one intentional live-test
   skip. Parent PostgreSQL tests passed 69; the later warning privacy assertion
   passed its selected failure-injection check. At `49b16a8c`, all 35 affected
   card tests, Web TypeScript and changed-file lint passed. Backend TypeScript
   passed. Existing React act warnings were recorded, not described as absent.
   These counts do not establish visual craft or a 98-point score.

## Open gates and next bounded outcomes

1. **Live Memory:** the original run, its explicit retry and a fresh Session
   all failed with `MODEL_RUN_FAILED`; the fresh probe took about 62 seconds
   and produced zero proposals. Stop blind retries. Diagnose using bounded,
   allowlisted metadata, then prove proposal → human selection → canonical
   Memory → correction/undo. The cause is not yet established.
2. **Runtime delivery:** storage last measured 64 GiB, below the 80 GiB
   heavy-build minimum. After the gate is satisfied, perform the required
   backend deployment and prove concurrent API responsiveness, source admission
   and stop/restart/prioritization on that deployed revision. The last observed
   resident backend was `973e7913-platform`; it is not current-code proof.
3. **Durable recovery:** unknown outcomes still lose their in-page reconciliation
   entry after refresh/close/Back. The late-write race is fixed, but truthful
   copy does not replace account/intent-bound durable reconciliation with an
   explicit retention/deletion policy.
4. **Remaining experience:** clarify Sources → Person → Memory → Today/Pursuit;
   validate populated/error states, long content, assistive use and native
   boundaries where relevant. The sidebar focus inset and latest candidate
   explanation still need visual readback. SessionWorkbench has an unrated
   relative-time hydration observation requiring a concrete reproduction.
5. **Credential incident:** cancelled Pi task `20260924-183413-d52d027f` printed
   an inherited API credential during an unsafe environment diagnostic, before
   any code edits. Its private evidence/accounting are preserved; never resume
   it or reuse its raw session. Subsequent implementation/review used a fresh
   or unaffected context and an allowlisted inherited environment. This does
   not undo the prior exposure. Key ownership/rotation follow-up is pending;
   no credential was changed. Do not expand the audit into unrelated credentials.

## Evidence and scoring

The [journey acceptance ledger](../docs/evaluations/experience-98/acceptance.md)
owns the atomic checks and missing evidence. Completion 25, recovery 25,
clarity 15, visual craft 15, accessibility 15, responsiveness 5. Unknowns cannot
pass; identity, privacy, loss, duplicate effects, false success and unauthorized
writes veto acceptance. The coarse five-point checks cannot distinguish 98
from 100. No numeric award or cross-journey average is issued. The historical
specialist panel is a parent self-review, not independent consensus.

The audit index retains nine deep optimization directions. Curated repository
evidence has a SHA-256 manifest; review and command-result records name the
exact source revision and distinguish static review from executed tests.

## Paused-runtime cleanup and continuation

Task-owned Web 3410 was stopped. Compose project `experience-98-db` on 55451 was
stopped/removed with its volumes/network. Generated `next-env.d.ts` was restored.
No shared Simulator was started; the guard retained all three and deleted none.

Before removing the exact registered temporary directory through the storage
guard, 104 files were verified in a private 0600 continuation archive:
`/Users/cubxxw/.local/state/talent-experience-98/archived-artifacts/experience-98-checkpoint-20260924.tar.gz`
(SHA-256 `4941a189ea4025d8149d5b6481bec25e9d5a1acaae60d37c9a5b796c51cf7292`).
Do not publicly upload raw private artifacts. Curated repository evidence remains
intact. Resume requires recreating the task-owned disposable setup and checking
current runtime/account boundaries; do not reuse an expired Lab blindly.
