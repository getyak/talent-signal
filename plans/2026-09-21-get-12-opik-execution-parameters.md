# GET-12 execution parameters

Status: in progress; owner inputs complete, implementation and delivery active.
Updated: 2026-09-21. Baseline: `926157da`.
Issue: https://linear.app/getyak/issue/GET-12
Branch: `codex/get-12-opik-scope` in an isolated worktree.

## Outcome and boundary

Record the owner's execution parameters, locate the existing private Opik,
and bind configuration and downstream execution tasks to those decisions.
Support explicit unlimited monetary ceilings in the existing budget ledger.
GET-12 does not establish semantic improvement or completed deployment.
No paid product inference or candidate rollout has occurred in this task;
the separately authorized Pi coding worker performs implementation only.

## Decisions and evidence

The [dated parameter receipt](../docs/evaluations/2026-09-21-get-12/README.md)
owns the owner's CNY/unlimited response, production-inclusive scope, DeepSeek
Flash choice, endpoint/credential observations and downstream responsibilities.
No budget response remains outstanding. The receipt is not an active permit.

- Main was fetched and this isolated branch fast-forwarded to `926157da`.
  The original checkout's unrelated changes remain untouched.
- Linear readback confirmed `In Progress`, initial discovery comment
  `#comment-f8048e08`, and a subsequent dated comment quoting the owner's
  explicit budget answer. The operations API credential could not resolve
  GET-12; the authenticated browser supplies verified issue updates.
- Pi task `20260921-115820-1e505b13` owns budget implementation/tests and the
  optimizer README. Parent owns parameter evidence, operational docs, review
  and delivery. No recursive delegation or external side effects are assigned.
- Independent discovery review found no P0/P1; it confirmed the provider and
  judge compatibility gaps. Implementation requires a fresh independent review.
- Initial docs/architecture and whitespace checks passed. New changes will be
  checked again after integration. No simulator or temporary test stack is needed.

## Milestones and acceptance

1. **Complete:** obtain and record monetary parameters, existing instance and
   model/release choices without invented defaults.
2. **Complete:** implement explicit `amountMicros: "unlimited"` in run/monthly
   limits while retaining finite usage, pricing, per-call bounds, resource
   limits, shared monthly policy, stop/revoke and unknown-outcome protection.
3. **Complete:** integrate Pi evidence, update GET-18/20 handoffs, independently
   review the actual diff, and run scoped runner checks plus `pnpm docs:check`.
4. **Active:** create PR, fix and pass current-head CI, merge, then read back
   the recorded parameters and Linear status before closing GET-12. Keep
   GET-11/18/20/21 open until their distinct execution acceptance is observed.

## Verification requirements

Prove old numeric permits still enforce their caps; unlimited is explicit and
survives SQLite reopen; absent/null/malformed money remains invalid; a finite
monthly cap cannot admit an unlimited run; concurrent/unknown calls retain
resource reservations; currency, pricing and model identity remain truthful.
The accepted scope is parameter/configuration delivery and monetary semantics.
DeepSeek subject/judge adaptation and a frozen live study belong to GET-18;
actual workspace UUID exposure binding and release/rollback belong to GET-20.

Integrated verification: runner typecheck and 112 focused tests passed. GET-18
and GET-20 handoff comments were read back, with links in the dated receipt.
Independent implementation review found no confirmed P0/P1/P2. Final local
documentation/architecture and whitespace checks passed.
