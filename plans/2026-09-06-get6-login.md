# GET-6 login experience

## Outcome

Deliver a brand-specific, responsive login experience based on GET-6's actual
reference images, with polished accessible motion and Google sign-in that
opens a real account-scoped backend session.

## Scope and ownership

Own the iOS authentication entry (primary), Web login adaptation, and minimum Google
authentication path and configuration required for a verified session. Preserve
all existing work, especially the extensive in-progress backend and iOS changes.
No candidate records, external messages, or unrelated product surfaces are in
scope. The user authorized browser operation and Google OAuth configuration.

## Evidence and unknowns

- Current login uses a static photograph and a form in two columns.
- Web Google OAuth is conditional on credentials, but currently provides no
  backend session. A successful Google callback alone does not grant access to
  account-scoped APIs.
- GET-6 was read in the already-authenticated Linear native app after browser
  and API access failed. The three supplied images are still captures of Wabi's
  mobile onboarding: swipe invitation, centered brand emergence, and portrait
  bubbles above two login providers. No original motion video was supplied.
- The issue explicitly requests email as the unique user-facing credential,
  Google support, swipe-led entry, soft portrait collisions, and a connected
  brand transition. The primary implementation is therefore native iOS.
- Infisical owns credentials. Do not save provider secrets in Git or output.

## Milestones

1. **Complete:** captured the native GET-6 references and authentication boundary.
2. **Complete:** verified the eleven-person reveal. First meeting contains no
   people or threads; swipe introduces an expressive burst, threads, then login.
3. **Complete:** Google iOS/Web clients, Infisical configuration, backend session
   authority, TestFlight local redeployment, and real Chrome Google readback.
4. **Complete:** native gesture/recovery and reduced-motion tests, light/dark
   renders, a nine-second screen recording, responsive Web inspection, focused
   tests, and documentation review. Google remains External / Testing.
   TestFlight publication is covered by the subsequently authorized release
   follow-up below; production consent-screen publication remains outside scope.

## Completion evidence

Preserve source-image analysis and rendered comparisons in a dated evaluation.
Require actual browser interaction and backend session readback for Google;
credentials present or a successful build are insufficient. Record any access
or infrastructure blocker separately from completed code.

## Review record

See [the dated evaluation](../docs/evaluations/2026-09-06-get6-login/README.md) for
source analysis, the rejected first composition, the user-selected choreography,
configuration boundaries, and verification evidence.

## Authorized release follow-up

The user explicitly requested commit, PR, merge, and a new version after the
implementation review. The release is isolated in `codex/get6-login-release`
from current `main` (`9873f22`); the original dirty workspace remains untouched.
Only the login, Google identity/session path, public client configuration,
illustration assets, and related evidence are included. Upstream lifecycle,
Calendar permission, migration history, localizations, and secret mappings are
preserved.

1. Complete: port and verify the focused change against current main.
2. Complete: commit and PR #142 passed required CI/Security gates, including
   461 iOS unit tests and nine isolated UI smoke journeys.
3. Complete: merged as `2f96e49` with tested-tree equality; deployed the exact
   backend and published TestFlight 0.1.60 (`20260906125812`).
4. Complete: verified the automation-owned processed-build receipt and a
   read-only internal access audit for the exact version/build (`VALID`,
   `SERVER_ACCESS_READY=true`). See the
   [release evidence](../docs/evaluations/2026-09-06-get6-login/release.md).

The existing workflow chooses the next unused patch version. Public App Store
submission and Google consent-screen production publication are out of scope.
