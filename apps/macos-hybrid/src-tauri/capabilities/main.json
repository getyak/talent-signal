{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main-capability",
  "description": "Main local workspace window. No shell, filesystem, URL-open, or remote Web permission.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
